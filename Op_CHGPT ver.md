# 🐍 Python — Конспект (1.5–1.12)

Конспект по базовым операциям, типам данных, вводу/выводу и условиям в Python с примерами задач и лаконичными решениями.

---

## 1.5 Операции с целыми числами

### 📌 Идея

* Целые числа (`int`)
* Арифметические операции: `+`, `-`, `*`, `//`, `%`, `**`
* Генерация чисел через строки

### 🧠 Пример

Вычислить произведение:

```
11111 · 1111111
```

(5 единиц и 7 единиц)

```python
print(int('1' * 5) * int('1' * 7))
```

🔎 `"1" * 5 → "11111" → int`

---

## 1.6 Операции с вещественными числами

### 📌 Экспоненциальная запись

Формат:

```
a.bcdeN  ⇔  a.bc × 10ᴺ
```

### 🧠 Пример

Записать `1.2345e3` как десятичное число:

```python
print(1.2345e3)
```

✅ Результат: `1234.5`

---

## 1.7 Типы данных

### 📌 Преобразование типов

```python
print(int(2.99))   # 2
print(int(-1.6))   # -1
```

⚠️ `int()` **отбрасывает дробную часть**, а не округляет

---

## 1.8 Переменные и стандартный ввод/вывод

### 📌 input / print

* `input()` — всегда строка
* `int(input())`, `float(input())`

---

### 🧠 Задача 1: Будильник (Коля)

⏰ Лечь в 00:00, уснуть через `X` минут

```python
print(*divmod(int(input()), 60), sep='\n')
```

📎 `divmod(X, 60) → (часы, минуты)`

---

### 🧠 Задача 2: Будильник (Катя)

⏰ Лечь в `H:M`, проснуться через `X` минут

```python
a, b, c = (int(input()) for _ in range(3))
print('%s\n%s' % ((a + c)//60 + b, (a + c)%60))
```

---

## 1.10 Условия: `if`, `elif`, `else`

### 📌 Логические операторы

* `<`, `<=`, `>`, `>=`, `==`
* `and`, `or`, `not`

---

### 🧠 Задача: режим сна

```python
a, b, h = (int(input()) for _ in range(3))
print('Это нормально'*(a <= h <= b) + 'Недосып'*(h < a) + 'Пересып'*(h > b))
```

✅ Используется умножение строк на `True / False`

---

## 1.12 Задачи по материалам недели

---

### 🔺 Площадь треугольника (формула Герона)

```python
exec(
    "print(((({0}+{1}+{2})/2)*(({0}+{1}+{2})/2-{0})*(({0}+{1}+{2})/2-{1})*(({0}+{1}+{2})/2-{2}))**(1/2))"
    .format(float(input()), float(input()), float(input()))
)
```

📌 Формула:

```
S = √(p(p-a)(p-b)(p-c)),  p = (a+b+c)/2
```

---

### ✅ Проверка попадания в интервалы

```python
n = int(input())
print(-15 < n <= 12 or 14 < n < 17 or 19 <= n)
```

---

### 🧮 Простой калькулятор

```python
a, b, op = input(), input(), input()
print(
    "Деление на 0!" if op in ("/", "div", "mod") and float(b) == 0
    else eval("({0}){1}".format(a, {'pow':'**','div':'//','mod':'%'}.get(op, op) + b))
)
```

⚠️ `eval()` — мощно, но небезопасно

---

### 📐 Площадь фигур (Малевия)

```python
f = input()
S = {
    'треугольник': [3, lambda a, b, c: (((a+b+c)/2)*(((a+b+c)/2)-a)*(((a+b+c)/2)-b)*(((a+b+c)/2)-c))**0.5],
    'прямоугольник': [2, lambda a, b: a*b],
    'круг': [1, lambda r: 3.14*r**2]
}
args = [float(input()) for _ in range(S[f][0])]
print(S[f][1](*args))
```

---

### 🔢 Максимум, минимум, середина

```python
x = sorted([int(input()), int(input()), int(input())])
print(x[2], x[0], x[1], sep='\n')
```

---

### 🤖 «n программистов» (склонения)

**Вариант 1**

```python
dic, n = {'1': '', '2': 'а', '3': 'а', '4': 'а'}, input()
print(n, 'программист'+(dic.get(n[-1], 'ов') if not 10 < int(n[-2:]) < 20 else 'ов'))
```

**Вариант 2 (арифметический)**

```python
n = int(input())
print(n, 'программист' + (
    'ов' if n%10 == 0 or 4 < n%10 < 10 or 10 < n%100 < 15
    else 'а' if 1 < n%10 < 5 else ''
))
```

---

### 🎟️ Счастливый билет

```python
n = int(input())
print(
    "Счастливый" if n//100000 + n//10000%10 + n//1000%10
    == n//100%10 + n//10%10 + n%10
    else "Обычный"
)
```

---

## ✅ Итог

Этот набор задач покрывает:

* ввод/вывод
* арифметику
* условия
* логические выражения
* строки и форматирование
* нетривиальные трюки Python

📌 Отличный базовый минимум для уверенного старта.
